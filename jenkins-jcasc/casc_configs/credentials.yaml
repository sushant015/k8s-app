credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: github-token
              username: "${GITHUB_USERNAME}"
              password: "${GITHUB_TOKEN}"
              description: GitHub Credentials