# Copy to .env and adjust if needed (optional — docker-compose.yml has defaults)
POSTGRES_USER=votepoll
POSTGRES_PASSWORD=changeme
POSTGRES_DB=votepoll
JWT_SECRET=local-dev-jwt-secret-key-min-32-chars!!
